Changes in component 'nf-core/star/starsolo'
Changes in 'star/starsolo/main.nf':
--- modules/nf-core/star/starsolo/main.nf
+++ modules/nf-core/star/starsolo/main.nf
@@ -18,6 +18,7 @@
     tuple val(meta),  path('*Log.out')           , emit: log_out
     tuple val(meta),  path('*Log.progress.out')  , emit: log_progress
     tuple val(meta),  path('*/Gene/Summary.csv') , emit: summary
+    tuple val(meta),  path('*.bam')              , emit: bam, optional: true
     tuple val("${task.process}"), val('star'), eval('STAR --version | sed -e "s/STAR_//g"'), topic: versions, emit: versions_star
 
     when:

'modules/nf-core/star/starsolo/environment.yml' is unchanged
Changes in 'star/starsolo/meta.yml':
--- modules/nf-core/star/starsolo/meta.yml
+++ modules/nf-core/star/starsolo/meta.yml
@@ -112,6 +112,20 @@
           pattern: "*/Gene/Summary.csv"
           ontologies:
             - edam: http://edamontology.org/format_3752 # CSV
+  bam:
+    - - meta:
+          type: map
+          description: |
+            Groovy Map containing sample information.
+            Here, you should add all the specific barcode/umi
+            information for each sample.
+            e.g. `[ id:'test_starsolo', umi_len:'12', cb_start:1 ]`
+      - "*.bam":
+          type: file
+          description: Alignments, written only when `--outSAMtype` in `ext.args` asks for a BAM.
+          pattern: "*.bam"
+          ontologies:
+            - edam: http://edamontology.org/format_2572 # BAM
   versions_star:
     - - ${task.process}:
           type: string

'modules/nf-core/star/starsolo/tests/main.nf.test' is unchanged
'modules/nf-core/star/starsolo/tests/main.nf.test.snap' is unchanged
'modules/nf-core/star/starsolo/tests/NO_FILE' is unchanged
************************************************************
